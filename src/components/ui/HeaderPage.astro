---
const { title, subtitle = "", color = "blue" } = Astro.props;
---

<header class={`page-header ${color}-header`}>
  <div>
    <h1>{title}</h1>
    {subtitle && <p>{subtitle}</p>}
  </div>
  <div>
    <slot />
  </div>
</header>
